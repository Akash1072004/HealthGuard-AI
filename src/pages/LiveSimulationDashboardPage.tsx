import LiveSimulationDashboard from "@/components/sections/LiveSimulationDashboard";

const LiveSimulationDashboardPage = () => {
  return <LiveSimulationDashboard />;
};

export default LiveSimulationDashboardPage;